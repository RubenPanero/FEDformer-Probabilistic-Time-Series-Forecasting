# Repository Docs

This directory contains versioned technical documentation for the current
repository state.

Current tracked documents:

- [`repository_technical_status.md`](repository_technical_status.md):
  optimization contract, benchmark budgets, validation commands, and platform
  expectations for the current branch/repo state.

Local-only documentation remains ignored by `.gitignore`:

- `docs/plans/`
- `docs/superpowers/`

Versioned visual assets used by the repository README now live under:

- `docs/assets/`

The removed `docs/optimization/` tree contained stale optimization material
that no longer matched the versioned code and tests.
